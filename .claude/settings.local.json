{
  "permissions": {
    "allow": [
      "Skill(update-config)",
      "Skill(update-config:*)",
      "Bash(jq -e '.hooks.PostToolUse[] | select\\(.matcher == \"\"Edit|Write\"\"\\) | .hooks[] | select\\(.type == \"\"command\"\"\\) | .command' \"C:/Users/liayang/Desktop/Google_Sheets_bk/【自动化】SMBC记账/.claude/settings.local.json\")"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(jq -r '.tool_input.file_path // empty' 2>/dev/null); [[ \"$file\" == *\"code.gs\" ]] && echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PostToolUse\",\"additionalContext\":\"code.gs was just edited. If the change affects functionality, config, classification rules, or behavior, update README.txt now.\"}}' || true",
            "statusMessage": "Checking if README needs update..."
          }
        ]
      }
    ]
  }
}
